-- | The player itself. For now it starts and exits; the screens, the Subsonic
-- client and the audio backend land on top of this module.
module Havidrome (run) where

-- | Runs the player to completion.
run :: IO ()
run = pure ()
